on:
  use: rss
  with:
    url: https://actionsflow.github.io/test-page/hn-rss.xml
  debug: true
steps:
  - run: |
      echo title: ${{on.event.title}}
      echo contentSnippet: $contentSnippet
      echo link: $link
